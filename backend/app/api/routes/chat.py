"""Chat endpoint with SSE streaming — stub for Backend Builder agent."""

from fastapi import APIRouter

router = APIRouter()

# TODO: Implemented by Backend Builder agent
# Endpoint:
#   POST /chat  — accepts ChatRequest, returns SSE stream
#   Events: token, sources, done, error
