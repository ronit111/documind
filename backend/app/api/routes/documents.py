"""Document CRUD endpoints — stub for Backend Builder agent."""

from fastapi import APIRouter

router = APIRouter(prefix="/documents")

# TODO: Implemented by Backend Builder agent
# Endpoints:
#   POST /upload    — upload file, validate, save, trigger processing
#   GET  /          — list all documents with status
#   GET  /{id}      — get document details
#   DELETE /{id}    — delete document + associated vectors
